<!-- partial--> 

    <!--<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <script language="javascript" type="text/javascript">
    function dynamicdropdown(listindex)
    {
        switch (listindex)
        {
        case "car_domain_nodrag" :
            document.getElementById("action").options[0]=new Option("Select action","");
            document.getElementById("action").options[1]=new Option("ACCELERATE","accelerate");
            document.getElementById("action").options[2]=new Option("DECELERATE","decelerate");
            break;
        case "" :
            document.getElementById("action").options[0]=new Option("Select action","");
            document.getElementById("action").options[1]=new Option("OPEN","open");
            document.getElementById("action").options[2]=new Option("DELIVERED","delivered");
            document.getElementById("action").options[3]=new Option("SHIPPED","shipped");
            break;
        }
        return true;
    }
    </script>
    </head>
    <title>Dynamic Drop Down List</title>
    <body>
       <p> <% var file1= JSON.stringify(global.file1.name) %>
                <% file1 = file1.slice(1,-6) %>
                file1 = <%= file1 %>
                <br>
                <% var file2 = JSON.stringify(global.file2.name) %>
                <% file2 = file2.slice(1,-6) %>
                file2 = <%= file2 %> </p>

    <div class="category_div" id="category_div">Source:
        <select id="source" name="source" onchange="javascript: dynamicdropdown(this.file1)">
        <option value="">Select source</option>
        <option value="manual">MANUAL</option>
        <option value="online">ONLINE</option>
        </select>
    </div>
    <div class="sub_category_div" id="sub_category_div">action:
        <script type="text/javascript" language="JavaScript">
        document.write('<select name="action" id="action"><option value="">Select action</option></select>')
        </script>
        <noscript>
        <select id="action" name="action">
            <option value="open">OPEN</option>
            <option value="delivered">DELIVERED</option>
        </select>
        </noscript>
    </div>
    </body> -->
<html> 
       <p> <% var file1= JSON.stringify(global.file1.name) %>
                <% file1 = file1.slice(1,-6) %>
                file1 = <%= file1 %>
                <br>
                <% var file2 = JSON.stringify(global.file2.name) %>
                <% file2 = file2.slice(1,-6) %>
                file2 = <%= file2 %> </p>
</html>
<!--<form>
    <% switch (file1){
        case "car_domain_nodrag": %>
        <select>
            <option value="">Select source</option>
            <option value="manual">MANUAL</option>
            <option value="online">ONLINE</option>
        </select>
    <input type="submit" value="show">
    <% break;
        case "gen_events_domain": %>
        
            <select>
            <option value="">Select source</option>
            <option value="manual">MANUAL</option>
            <option value="online">ONLINE</option>
        </select>
    <input type="submit" value="show">
        
    <% break;
        default: %>
        <p></p>
        <%  break; } %> 
</form>-->